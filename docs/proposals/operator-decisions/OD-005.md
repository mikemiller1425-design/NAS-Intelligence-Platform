# OD-005 — Database location and backup policy

> **Non-authoritative decision brief.** It prepares a decision; it does not make one.
> **Claude has not decided this and must not.**

| Field | Value |
| --- | --- |
| Decision | OD-005 |
| Severity | BLOCKER |
| Blocks gate | `dry_run` |
| Reconfirm at | `live` |
| Review session | 3 — Session 3 — before the first live read (G4) |
| Complexity | low |
| Earliest blocked rung | FBL-013 |
| All blocked rungs | FBL-013, FBL-071 |

## The decision being requested

Where does control data live, and what is its backup policy?

## Why it matters

The control root must sit outside every scanned source path and must be on a volume with a strong durability class — otherwise mutation is prohibited entirely.

## What the operator must supply

A control-data root path, its durability class, and a backup policy.

## Default behaviour if unresolved

Fixture control storage suffices. No dry run.

## Evidence required to close

A written answer recorded against OD-005 in `docs/05-governance/open-decisions.md`, with the
supplying evidence attached.

## Register note (verbatim, for context)

> SQLite + JSONL recommended; path and backup unset. Control-data storage must sit outside every recursively scanned source root.

## Prerequisite reading

- `docs/05-governance/open-decisions.md` — the register entry for OD-005
- `docs/handoffs/build-ladder.md` — the rungs listed above
