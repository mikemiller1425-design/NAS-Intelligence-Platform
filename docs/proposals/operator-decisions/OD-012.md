# OD-012 — Confirm Dogs / drone / CSV / identity-candidate rules

> **Non-authoritative decision brief.** It prepares a decision; it does not make one.
> **Claude has not decided this and must not.**

| Field | Value |
| --- | --- |
| Decision | OD-012 |
| Severity | MAJOR |
| Blocks gate | `dry_run` |
| Reconfirm at | — |
| Review session | 3 — Session 3 — before the first live read (G4) |
| Complexity | high |
| Earliest blocked rung | FBL-071 |
| All blocked rungs | FBL-071 |

## The decision being requested

Confirm the Dogs, drone, CSV, and identity-candidate rules.

## Why it matters

The rule set must be frozen and confirmed before evaluation against real data. Until then every provisional rule is structurally advisory-only and cannot produce an executable outcome. **Note:** closing this decision would break the shipped example rule set unless promotion and closure are made one atomic change — that is specification defect PF-16, not this decision.

## What the operator must supply

The actual classification rules, including whether Dogs genuinely outranks People when both appear.

## Default behaviour if unresolved

Provisional rules remain advisory-only. No dry run.

## Evidence required to close

A written answer recorded against OD-012 in `docs/05-governance/open-decisions.md`, with the
supplying evidence attached.

## Register note (verbatim, for context)

> Handwritten transcriptions missing; examples are provisional. The rule set must be frozen and confirmed before evaluation against real data. This is the operator decision referenced by audit finding FND-B003.

## Prerequisite reading

- `docs/05-governance/open-decisions.md` — the register entry for OD-012
- `docs/handoffs/build-ladder.md` — the rungs listed above
