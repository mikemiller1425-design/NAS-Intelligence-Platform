# OD-002 — Destination taxonomy freeze

> **Non-authoritative decision brief.** It prepares a decision; it does not make one.
> **Claude has not decided this and must not.**

| Field | Value |
| --- | --- |
| Decision | OD-002 |
| Severity | BLOCKER |
| Blocks gate | `dry_run` |
| Reconfirm at | `live` |
| Review session | 3 — Session 3 — before the first live read (G4) |
| Complexity | medium |
| Earliest blocked rung | FBL-034 |
| All blocked rungs | FBL-034, FBL-071 |

## The decision being requested

Is the destination taxonomy frozen, and in what form?

## Why it matters

A dry run emits destination proposals, so the taxonomy must be frozen before it runs. Note the taxonomy also has no machine-readable schema (PF-15) — that is a specification defect to fix first, and is not this decision.

## What the operator must supply

A frozen taxonomy version, with node names and boundaries.

## Default behaviour if unresolved

Taxonomy schema work proceeds; freezing does not.

## Evidence required to close

A written answer recorded against OD-002 in `docs/05-governance/open-decisions.md`, with the
supplying evidence attached.

## Register note (verbatim, for context)

> Existing vs proposed taxonomy; folder boundaries; naming. Dry run emits destination proposals, so the taxonomy must be frozen first.

## Prerequisite reading

- `docs/05-governance/open-decisions.md` — the register entry for OD-002
- `docs/handoffs/build-ladder.md` — the rungs listed above
