# NEGATIVE EXAMPLE — MUST FAIL VALIDATION
# Case: retired-flat-schema
# Expected failure: The retired flat rule schema must be rejected wholesale.
# Generated by scripts/generate_negative_rule_fixtures.py — do not hand-edit.
version: 1.0.0-example
status: non_production_example
evaluation_order:
- safety_exclusions
rules:
- id: MEDIA-100-dogs
  enabled: true
  confidence_threshold: 0.9
  review_required: false
  destination: /fixtures/destination/x
  applicable_file_types:
  - image
  metadata_conditions: {}
  visual_content_conditions: {}
  fallback: manual_review
